node_modules/
.env
coverage/
palette-utils-ts/coverage/

# macOS folder metadata (any directory)
.DS_Store

# Persisted UI state (per-user; app writes on change)
app_state.json
app-status.json

# Local-only Cursor command (do not push)
.cursor/commands/env.md